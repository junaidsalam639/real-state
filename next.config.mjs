/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     domains: ['images.unsplash.com', 'cdn.pixabay.com', 'images.pexels.com', 'via.placeholder.com'],
    // }
};

export default nextConfig;
